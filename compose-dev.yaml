services:
  app:
    entrypoint:
    - sleep
    - infinity
    image: env/jdk8:latest
    init: true
    volumes:
    - type: bind
      source: /var/run/docker.sock
      target: /var/run/docker.sock

